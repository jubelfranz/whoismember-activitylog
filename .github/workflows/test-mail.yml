name: Connectivity Receiver
on:
  repository_dispatch:
    types: [test_mail_trigger]
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Acknowledge
        run: echo "Signal received from Build-Hub"
